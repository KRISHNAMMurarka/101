{
  "$comment": "Explicitly reviewed advisories that are reachable only from developer build tooling, never from code shipped to a player. Every entry must name the dependency path, state whether the vulnerable code path is reachable, and carry a reviewBy date. Anything not listed here fails the gate, so a new advisory can never be absorbed silently. See docs/privacy-security.md.",
  "reviewed": [
    {
      "package": "image-size",
      "advisory": "image-size ICNS/JXL/HEIF parsers allow denial of service through infinite loops",
      "severity": "high",
      "path": "@101/controller-native > expo > @expo/metro > metro > image-size",
      "surface": "build-tooling",
      "justification": "Metro measures image assets while bundling the native app on a developer machine. It never runs on a player device and is not part of the web runtime or the shipped iOS/Android bundle. The parser only sees asset files committed to this repository, so there is no untrusted input path.",
      "vulnerableCodePathReachable": true,
      "fixAvailable": false,
      "fixNotes": "Every published image-size version is in the vulnerable range, so no upgrade resolves this. npm proposes downgrading expo 57 to 53, a semver-major downgrade that would break the verified native Link build. Re-check for a patched image-size release at the review date.",
      "reviewed": "2026-08-13",
      "reviewBy": "2026-11-13"
    },
    {
      "package": "uuid",
      "advisory": "uuid missing buffer bounds check in v3/v5/v6 when buf is provided",
      "severity": "moderate",
      "path": "@101/controller-native > expo > @expo/config-plugins > xcode > uuid",
      "surface": "build-tooling",
      "justification": "The xcode package rewrites Xcode project files during iOS prebuild on a developer machine. It calls only uuid.v4(); the advisory concerns v3/v5/v6 called with an explicit buf argument, so the vulnerable code path is not reached.",
      "vulnerableCodePathReachable": false,
      "fixAvailable": true,
      "fixNotes": "Patched in uuid >= 11.1.1, but the fix lives inside a transitive dependency of Expo's own tooling. Forcing an override into xcode's internals risks breaking iOS project generation for an unreachable code path. Adopt it when Expo updates xcode upstream.",
      "reviewed": "2026-08-13",
      "reviewBy": "2026-11-13"
    },
    {
      "package": "@xmldom/xmldom",
      "advisory": "xmldom: XML fragment injection via invalid EntityReference.nodeName during requireWellFormed serialization",
      "severity": "moderate",
      "path": "@101/controller-native > expo > @expo/cli > @expo/plist > @xmldom/xmldom, and @101/controller-native > expo > @expo/cli > @expo/config-plugins > xcode > simple-plist > plist > @xmldom/xmldom",
      "surface": "build-tooling",
      "justification": "Both paths are Expo prebuild tooling that runs on a developer machine: @expo/plist and simple-plist read and rewrite Info.plist and entitlements while generating the iOS project. Neither ships in the web runtime or in the built iOS/Android bundle. The only XML they serialize is this repository's own generated app configuration, so no attacker-controlled document reaches the vulnerable path.",
      "vulnerableCodePathReachable": false,
      "fixAvailable": true,
      "fixNotes": "Patched above 0.9.11, but both copies are pinned inside Expo's own CLI dependency tree. Forcing an override into plist generation risks breaking iOS prebuild for a path that only ever sees our own files. Adopt when Expo updates @expo/plist and xcode upstream.",
      "reviewed": "2026-08-16",
      "reviewBy": "2026-11-16"
    },
    {
      "package": "fast-uri",
      "advisory": "fast-uri host confusion and SSRF via IDN canonicalization, IPv6 normalization and percent-decoding",
      "severity": "high",
      "path": "@101/controller-native > expo > @expo/cli > @expo/router-server > react-server-dom-webpack > webpack > schema-utils > ajv-formats > ajv > fast-uri",
      "surface": "build-tooling",
      "justification": "ajv uses fast-uri to resolve $ref URIs while validating webpack loader and plugin option schemas during bundling on a developer machine. The URIs it parses come from webpack's own schema files, not from the network or from user input, and none of this runs on a player device or in the shipped bundle. The SSRF and host-confusion classes require attacker-controlled URIs, which this path does not have.",
      "vulnerableCodePathReachable": false,
      "fixAvailable": true,
      "fixNotes": "Patched above 3.1.5. It sits four levels inside Expo's router-server tooling; an override would pin a transitive dependency of webpack's schema validator. Adopt when ajv or webpack updates upstream, and re-check at the review date.",
      "reviewed": "2026-08-16",
      "reviewBy": "2026-11-16"
    }
  ]
}
