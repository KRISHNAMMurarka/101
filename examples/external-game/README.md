# External game example

This directory models a game developed outside the 101 repository. Its one package export combines the gameplay lifecycle, semantic input contract, launcher metadata, and universal Link controller role. A host can validate, register, and launch it without adding game-specific networking or controller code.
